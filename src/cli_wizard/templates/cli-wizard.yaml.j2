# CLI Wizard Configuration
# All parameters use PascalCase naming convention.
# Reference other parameters with #[ParamName] syntax.
# Reference environment variables with ${VAR} syntax.

{% for field in _schema_fields %}
# {{ field.description }}
{% if field.type_hint %}
# Type: {{ field.type_hint }}
{% endif %}
{% set value = _values.get(field.name, field.default) %}
{% if field.name in _prompted_params %}
{{ field.name }}: {{ value | yaml_value }}
{% else %}
# Default: {{ field.default | yaml_value }}
# {{ field.name }}: {{ field.default | yaml_value }}
{% endif %}

{% endfor %}
