Installation
============

Install the CLI Wizard using pip:

.. code-block:: bash

   pip install cli-wizard

Or install from source:

.. code-block:: bash

   git clone https://github.com/gmarciani/cli-wizard.git
   cd cli-wizard
   pip install -e .

Configuration
-------------

Verify the installation:

.. code-block:: bash

   cli-wizard --version
