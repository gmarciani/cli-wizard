[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my_cli"
dynamic = ["version"]
description = "A CLI application"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Firstname Lastname", email = "firstname.lastname@gmail.com"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
requires-python = ">=3.12"
dependencies = [
    "click~=8.4",
    "PyYAML~=6.0",
    "requests~=2.34",
]

[project.scripts]
my-cli = "my_cli.cli:main"

[project.urls]
Homepage = "https://github.com/gmarciani/my-cli"
Repository = "https://github.com/gmarciani/my-cli"

# Local-only tooling (PEP 735). Groups are never published, so none of this
# reaches anyone installing my_cli from an index. There are no extras:
# the built distributions carry no tests, so a published test extra would only
# install pytest for a consumer with nothing to run it against.
[dependency-groups]
dev = [
    {include-group = "test"},
    {include-group = "docs"},
    "build~=1.5",
    "mypy~=2.3",
    "pre-commit~=4.6",
    "ruff~=0.16.2",
    "tox~=4.58",
    "types-PyYAML~=6.0",
    "types-requests~=2.33",
]
test = [
    "pytest~=9.1",
    "pytest-cov~=7.1",
]
docs = [
    "sphinx~=9.1",
    "sphinx-click~=6.2",
    "sphinx-rtd-theme~=3.1",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["my_cli*"]

[tool.setuptools.package-data]
my_cli = ["resources/*"]

[tool.setuptools.dynamic]
version = {file = "VERSION"}

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = ["click.*", "requests.*"]
ignore_missing_imports = true

[tool.coverage.run]
omit = ["*/__init__.py"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
